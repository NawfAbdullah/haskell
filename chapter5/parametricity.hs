-- Parametricity

-- 1) x :: a->a; x a = a + a; error
-- 2) x :: a -> a -> a; x c d = c 
-- 3) x :: a -> b -> b; x c d = d; x c d = d * d
-- it can have more than one implementation and changing types of a or b does not change as along as it's an identity Functions

